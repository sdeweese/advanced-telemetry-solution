[[processors.lookup]]
  ## List of files containing the lookup-table
  files = ["/etc/telegraf/telegraf.d/references/building.json"]
  key = '{{.Tag "switch"}}'

  [processors.lookup.tagpass] 
    bucket_tag = ["cisco_mdt"]
